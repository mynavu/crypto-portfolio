import { Address, AccountMeta, AccountSignerMeta, Instruction, TransactionSigner } from "@solana/kit";
export declare const DISCRIMINATOR: Buffer<ArrayBuffer>;
export interface CloseEmptyUserStateAccounts {
    /**
     * The account that signs the transaction
     * - Non-delegated: user signs
     * - Delegated: delegated authority signs
     */
    signer: TransactionSigner;
    userState: Address;
    /** The farm state account for validation */
    farmState: Address;
    /**
     * The account that receives the rent
     * - Non-delegated: user receives the rent
     * - Delegated: farm admin receives the rent
     */
    rentReceiver: Address;
    systemProgram: Address;
}
export declare function closeEmptyUserState(accounts: CloseEmptyUserStateAccounts, remainingAccounts?: Array<AccountMeta | AccountSignerMeta>, programAddress?: Address): Instruction<string, readonly (import("@solana/kit").AccountLookupMeta<string, string> | AccountMeta<string>)[]>;
//# sourceMappingURL=closeEmptyUserState.d.ts.map